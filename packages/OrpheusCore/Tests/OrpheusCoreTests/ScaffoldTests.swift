import XCTest
import OrpheusCore
import GRDB

final class ScaffoldTests: XCTestCase {
    func testLinkSucceeds() {
        // Proves OrpheusCore + GRDB link cleanly. No logic yet — Groups 2-8 fill this in.
        XCTAssertTrue(true)
    }
}
