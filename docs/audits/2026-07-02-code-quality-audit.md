# Code quality audit — 2026-07-02

Multi-agent audit of the full repo at staging tip `db0bd33`. Five wave-1 dimensions
(CI/quality gates, duplication/structure, performance, error handling, open-source
readiness) plus a wave-2 deep dive on code-quality patterns (appended below as it
lands). Every finding carries a stable ID so fixes/PRs can reference them.

Severity: **H** = high (user-visible harm, correctness, or gate-level gap),
**M** = medium (real cost, not urgent), **L** = low (hygiene).
Status: `open` until a fix lands; update in place.

Baseline at audit time: `bun run typecheck` ✅ clean · `bun run lint` ❌ **fails**
(1 error + 233 warnings — see ACT-1).

---

## 1. Actively broken / costing right now

| ID | Sev | Finding | Evidence | Status |
|---|---|---|---|---|
| ACT-1 | H | `bun run lint` fails on the current tree: 1 error + 233 warnings, nearly all from `docs/brainstorms/2026-07-02-panes-mockups/` which ESLint should never lint. Proof of the enforcement gap (CI-1/CI-10). | `docs/brainstorms/2026-07-02-panes-mockups/shared.js:101` (no-unused-vars error) + ~230 prettier warnings in same dir | open |
| ACT-2 | H | Unthrottled `console.warn` per session file per reconcile tick. `KNOWN_GOOD_VERSIONS = {'2.1.190'}` but installed claude is 2.1.198, so the warning fires N-files × (every fs event + every 2.5 s), continuously — thousands of synchronous stdout writes/hour on the main thread. `knownBadSessionFiles` dedups parse errors only, not version warnings. | `src/main/sessionState.ts:27`, `:330-334`, interval `:182` | open |
| ACT-3 | H | Dormant hook stack has machine-wide runtime cost: 9 hooks installed into global `~/.claude/settings.json` (incl. PreToolUse/PostToolUse) but 8 of 9 `handleHookEvent` cases are immediate no-op returns. Every tool call in **every** claude session on the machine spawns the `orpheus-notify` shim + Unix-socket roundtrip whose payload is discarded. Only `notification` does anything. The `session-start` case is a no-op — contradicting CLAUDE.md's claim that SessionStart→overlay-dismissal is still live (it's file-driven now). | `src/main/orpheusNotify.ts:25-35` (HOOK_EVENT_MAP), `:242-279` (dead switch), `:342-419` (install) | open |
| ACT-4 | M | Native addon takes `NSActivityUserInitiated \| NSActivityLatencyCritical \| NSActivityIdleSystemSleepDisabled` into a `__strong static` on first mount and never ends it — idle **system** sleep disabled for process lifetime once any terminal mounts, bypassing the careful opt-in policy in `src/main/powerAwake.ts` (`shouldHold()` auto mode). Real battery cost on laptops at idle. | `packages/ghostty-surface/addon.mm:2436-2441` | open |
| ACT-5 | M | `git:listCommits` is the one remaining `execFileSync` (with `--shortstat`, which forces per-commit diff computation; timeout 3 s) — blocks ALL main-thread work (IPC, reconcile, ghostty tick) while it runs. Every sibling in `git.ts` is already async `execFile`. | `src/main/git.ts:216,222`; IPC wire `src/main/index.ts:1987` | open |
| ACT-6 | M | First footer-chip fetch per workspace cold-builds the usage accumulator by reading + `JSON.parse`-ing the **entire** JSONL transcript synchronously on the main thread (transcripts can be tens of MB → hundreds of ms block). Incremental follow-ups and the 30 s TTL cache are fine; only the cold build (per workspace per app launch, and after eviction) is the problem. | `src/main/actions/session.ts:314-390` (`advanceAccumulator`), cache `:51` | open |

## 2. CI, automation, and quality gates

| ID | Sev | Finding | Evidence | Status |
|---|---|---|---|---|
| CI-1 | H | Direct pushes to `staging` run **zero** CI, yet staging is the sole working branch and receives direct commits (e.g. `db0bd33`). ci.yml triggers on `pull_request` + `push: main` only. | `.github/workflows/ci.yml:3-6` | open |
| CI-2 | H | `noImplicitAny` silently OFF despite `strict: true`: the extended `@electron-toolkit/tsconfig` sets `strict: true` then `noImplicitAny: false`; neither tsconfig re-enables it. ESLint `no-explicit-any: error` catches explicit `any` but not implicit. | `tsconfig.node.json`, `tsconfig.web.json` (both extend `@electron-toolkit/tsconfig`) | open |
| CI-3 | H | Native addon changes never compile-checked in CI: `native-build-check.yml` paths-filter covers only `scripts/fetch-libghostty.sh`; ci.yml installs with `--ignore-scripts`. An Obj-C++ syntax error in `addon.mm` merges green and fails at release-build time. | `.github/workflows/native-build-check.yml:16-19`; `ci.yml:31-35` | open |
| CI-4 | H | No tests and no test runner. Highest-value seams, in order: (a) `composeClaudeLaunch` (`src/main/claudeSettings.ts`) — pure layering with documented precedence invariants; (b) db migrations — `scripts/smoke-db-migrations.ts` exists but see CI-5; (c) status mapping in `src/main/sessionState.ts`; (d) settings validators/BOOLEAN_KEYS. `bun test` needs zero new deps. | no `*.test.*` anywhere; `ci.yml:57` comment acknowledges | open |
| CI-5 | M | DB-migration smoke test runs only at release time, **after** the tag exists (`build-and-attach`), not in PR/staging CI. A migration regression is caught post-release. | `.github/workflows/release-please.yml:200-206` | open |
| CI-6 | M | All release-quality gates (typecheck, vite build, smoke) run AFTER release-please created the tag + GitHub release; a failure leaves a half-published release requiring manual `release.yml` repair. Mitigate via CI-1/CI-5 running pre-merge. | `release-please.yml:158-206` | open |
| CI-7 | M | Break-glass `release.yml` builds default-branch HEAD, not the requested tag — checkout has `fetch-depth: 0` but no `ref: ${{ inputs.tag }}`; the version sanity gate passes in the common case, so a rebuild can silently include post-tag commits. | `.github/workflows/release.yml:48-50`, gate `:62-67` | open |
| CI-8 | M | No security scanning at all: no CodeQL, no dependency-audit job, no secret scanning. Notable for a source-available Electron app storing auth tokens. | `.github/workflows/` (six files, none security) | open |
| CI-9 | M | Actions pinned by mutable major tags (`actions/checkout@v7`, `oven-sh/setup-bun@v2`, `wagoid/commitlint-github-action@v6`, `googleapis/release-please-action@v5`) — third-party actions run in workflows holding `HOMEBREW_TAP_TOKEN`/`RELEASE_PLEASE_TOKEN`. Pin SHAs; dependabot keeps them fresh. | all workflows | open |
| CI-10 | M | Local hooks enforce Prettier only: lint-staged = prettier, pre-push = prettier --check. ESLint and typecheck enforced nowhere locally; no `commit-msg` commitlint hook (conventional-commit format unchecked for direct staging pushes). | `.husky/pre-commit`, `.husky/pre-push`, `package.json` lint-staged block | open |
| CI-11 | M | `bun-version: latest` floats the toolchain in every CI/release run — a bun regression can break a release with no repo change. | `ci.yml:29`, `native-build-check.yml:35`, `release-please.yml:187`, `release.yml:74`, `bump-libghostty.yml:29` | open |
| CI-12 | L | `scripts/check-overlay-discipline.mjs` and `scripts/verify-contrast.mjs` are quality guards nothing runs (not in CI, package.json, or husky). Both are pure-Node and `--ignore-scripts`-compatible. | grep across `.github/`, `package.json`, `.husky/` | open |
| CI-13 | L | CI check job runs on macOS (10× cost) with no dependency caching; `.eslintcache` never persists. ci.yml's own comment concedes ubuntu would do. | `ci.yml:21` | open |
| CI-14 | L | Tap publish can half-fail: plain `git push` after the immutable tap release is created, no rebase/retry on divergence → tap release exists with stale cask. | `release-please.yml:275-283` vs `:261-265` | open |
| CI-15 | L | Stale comment: release-please.yml claims the checkout "sees the bumped package.json + CHANGELOG.md" but config sets `"skip-changelog": true` and no CHANGELOG.md exists. | `release-please.yml:181-183`, `release-please-config.json` | open |
| CI-16 | M | ESLint is recommended-tier, not type-aware: no `no-floating-promises` / `no-misused-promises` — in a main process full of fire-and-forget async IPC handlers. Switch `src/main/**` to the type-checked config at minimum. | `eslint.config.mjs` | open |

Verified fine: PRs to staging DO run typecheck+lint+format; concurrency groups correct; dependabot covers npm + actions; release.yml has no auto trigger (the historical double-fire is fixed); commitlint relaxations are documented-deliberate.

## 3. Duplication, dead code, structure

| ID | Sev | Finding | Evidence | Status |
|---|---|---|---|---|
| DUP-1 | H | Whole-file twin: `claudeProjectSettings.ts` vs `claudeWorkspaceSettings.ts` — both 110 lines, **zero diff** after project/workspace substitution. Should be one generic `overridesStore(table, idColumn)` factory. | `src/main/claudeProjectSettings.ts`, `src/main/claudeWorkspaceSettings.ts` | open |
| DUP-2 | H | Whole-file twin: `ClaudeSubagentsSection.tsx` (626) vs `ClaudeSlashCommandsSection.tsx` (630) — identical skeleton at near-identical line numbers; only ~276 of ~1256 lines diverge. Extract a generic frontmatter-file collection editor (list+group+form+CRUD) parameterized by field schema. | `src/renderer/src/components/dashboard/settings/` | open |
| DUP-3 | H | The IPC contract is stringly-typed and maintained in triplicate: 129 channel strings independently in main (`handle()` wrapper erases to `(...args:any[])=>any`), preload invoke wrappers, and the 428-line `index.d.ts`. A typo or payload drift compiles clean. Biggest structural win: shared typed `ChannelMap` in `src/shared/types.ts`, derive preload from it. | `src/main/index.ts:1017-1049`, `src/preload/index.ts`, `src/preload/index.d.ts` | open |
| DUP-4 | M | `ProjectRow` type + row→record mapper duplicated verbatim (incl. the `// v37` comment) between projects.ts and workspaces.ts — new project column requires 4 edits. | `src/main/projects.ts:14-48`, `src/main/workspaces.ts:~35-50,91-108` | open |
| DUP-5 | M | Settings-section load/optimistic-patch scaffold copy-pasted across 10+ section components (44 `let cancelled` hits renderer-wide). Extract `useGlobalSettings()` / `useUiState()` hooks (fetch + optimistic patch + reconcile). | `ClaudeGeneralSection.tsx:22-49`, `ClaudeMemorySection.tsx:14-37`, `OrpheusAppearanceSection.tsx:70`, +8 more | open |
| DUP-6 | M | Preload event-listener boilerplate ×24 (`ipcRenderer.on` + removeListener closure) alongside 129 hand-written invoke wrappers, mirrored again in index.d.ts. `subscribe<T>(channel)` helper; ideally solved together with DUP-3. | `src/preload/index.ts` (~78-84, ~110-118, ×24) | open |
| DUP-7 | L | `searchIndex.ts` (1,864 lines) hand-syncs every setting's label/description/mapsTo/keywords — guaranteed drift from the actual `SettingRow`s. Co-locate search metadata with setting declarations and build the index. | `src/renderer/src/components/dashboard/settings/searchIndex.ts` | open |
| DEAD-1 | H | (= ACT-3) Dormant hook stack: ~350 lines servicing one `broadcastDetailIfChanged`. Trim installed hooks to Notification-only or delete the stack. | `src/main/orpheusNotify.ts` | open |
| DEAD-2 | M | 78 exported symbols never imported elsewhere. Top removals: ~35 unused exports in `dotmatrix-core-lib.ts` (668 lines); `dotm-square-12.tsx` (component unused); `dotmatrix-factory.tsx` `createPathWaveComponent`; `activitySink.ts:43-52` legacy fan-out loop iterating a **provably empty** listener set (`onActivityChange` imported nowhere); `overlayDevTest.ts` (133 lines); `getWorkspaceForkedFromSessionId`, `parseBrewLine`, `serializeGhosttyConfig`, `fetchStatusSnapshot`, CLI `EXIT_CODES`/`ENV_VARS`. (Some are internal-use — only the `export` keyword is dead.) | script scan over src + packages | open |
| STR-1 | H | God files: `src/main/index.ts` 3,217 lines / 129 `ipcMain.handle` registrations (sectioned but not modular; only `actions/terminal.ts` self-registers). Split into per-domain `registerXxxIpc(deps)` modules. | `src/main/index.ts` | open |
| STR-2 | M | God components: `Dashboard.tsx` 1,502 lines (13 useState, 22 useEffect, 78 `window.api` sites — routing + terminal rects + activity + settings + dirty-chip), `Sidebar.tsx` 1,458 (21 useState; drag-reorder + pinning + context menus + IPC inline), `ClaudeToolsSection.tsx` 945, `ClaudeDeveloperSection.tsx` 908, `WorkspaceView.tsx` 823. | `src/renderer/src/components/dashboard/` | open |
| STR-3 | M | Type-safety leak clusters (repo discipline otherwise good: 2 `: any`, 3 `as any`, 2 `@ts-ignore`, 19 `as unknown as`): (a) DUP-3's IPC erasure; (b) 45 `.get() as` + 21 `.all() as` sqlite casts against per-file Row types; (c) all 8 overlay kinds round-trip props through `Record<string,unknown>` + `as unknown as XProps` both directions (`overlayClient.ts` casts ×9) — wants a discriminated `OverlayKindPropsMap` in shared types. | `overlay/kinds/*.tsx:~21`, `overlayClient.ts:248-536` | open |

## 4. Performance

| ID | Sev | Finding | Evidence | Status |
|---|---|---|---|---|
| PERF-1 | H | = ACT-2 (version-warning spam on hot reconcile path). | `sessionState.ts:330` | open |
| PERF-2 | H | = ACT-5 (`execFileSync` listCommits blocks main thread ≤3 s). | `git.ts:222` | open |
| PERF-3 | M | = ACT-4 (process-lifetime NSActivity defeats powerAwake policy). Scope to "≥1 surface attached and not occluded" (addon already tracks both) or drop `IdleSystemSleepDisabled`. | `addon.mm:2436` | open |
| PERF-4 | M | = ACT-6 (sync whole-transcript cold read for usage chips). Chunk with `setImmediate` yields (pattern exists in sessions.ts) or bound the cold read like the sessions scanner's 200 KB head/tail. | `actions/session.ts:314-390` | open |
| PERF-5 | M | Shared-repo git refresh duplicates work N×: `refreshGitForDir` awaits `getGitStatus(cwd)` **per client**, but all clients of a watcher entry share the same resolved cwd; each call spawns 4 git subprocesses → 4K spawns per debounced change with K workspaces on one repo. Compute once, fan out. | `src/main/git.ts:293-336`, dedup key `:405`, spawns `:41-56` | open |
| PERF-6 | L | Fixed 2.5 s reconcile interval runs `readdirSync` + per-file `readFileSync` + a DB query forever, even with a healthy watcher and backgrounded app. Back off (10–30 s) when watcher alive, like claudeStatus.ts blur backoff. | `sessionState.ts:182-184` | open |
| PERF-7 | L | `[perf] eventloop` console.log every 10 s forever in production; self-described temporary instrumentation. Gate behind dev/diag flag. | `src/main/index.ts:2722-2731` | open |
| PERF-8 | L | `knownBadSessionFiles` prune is O(bad×files) via `files.includes` in a loop (use a Set); `deadPidReported` and LiveChip `chipValueCache` (module Map keyed actionId:workspaceId) never evict — clear alongside Dashboard's per-workspace purges on archive. | `sessionState.ts:320`, `:82`; `LiveChip.tsx:89`; `Dashboard.tsx:361-368` | open |

Verified fine (don't "fix"): sessions.ts JSONL scanning (bounded 200 KB reads, mtime guard, in-flight dedup, yields, single-transaction); 16 ms-coalesced activity IPC batches; per-key `useSyncExternalStore` stores + sharedTicker; ref-counted watcher lifecycles with archive teardown; hidden/occluded surfaces gate-stop the display link and the 10 Hz timer early-returns at idle; WAL + NORMAL + proper indexes; startup work deferred post-first-paint.

## 5. Error handling and robustness

Posture: a real diagnostics pipeline exists (in-memory ring → SQLite w/ retention → user-facing `diag:export`; renderer onerror/unhandledrejection captured; every IPC handler logs+rethrows via the `handle()` wrapper). The gaps are the paths that bypass it.

| ID | Sev | Finding | Evidence | Status |
|---|---|---|---|---|
| ERR-1 | H | Startup failure is fully silent: `app.whenReady().then(...)` has no `.catch`; the `unhandledRejection` listener (log-only) suppresses Node's default fatal exit; a `getDb()` throw (corrupt/locked sqlite, failed migration) → no window, no dialog, and the diag flush needs the same broken DB, so no persisted log either. Add `.catch` → `dialog.showErrorBox` + plaintext crash file under userData + `app.exit(1)`. | `src/main/index.ts:2698`, `:659-668`; `src/main/db.ts:399-418` | open |
| ERR-2 | H | `uncaughtException` handler converts main-process crashes into silent zombie state; its comment claims it "does NOT alter Electron's default crash handling" — registering the listener does exactly that. Rethrow/exit for fatal, or surface once; fix the comment. | `src/main/index.ts:648-658` | open |
| ERR-3 | H | Two-tier logging: ~25 real warn/error paths log via `console.*` only — invisible in the packaged app, absent from diag export. Includes the exact `setWorkspaceStatus` swallow that historically froze workspaces at "Claude is thinking" (memorialized at `db.ts:51-55`), watcher-fell-back-to-polling, hook install/parse failures, launch-settings failures. Route through `logDiagMain`. | `orpheusNotify.ts:213,357,472,487,552`; `sessionState.ts:250,257,289,399`; `index.ts:2796` | open |
| ERR-4 | H | Renderer has no user-visible error surface: no toast system exists; 35× `.catch(() => {})` + 70× `.catch(console.error)` in renderer. Failed archive/rename/add-project/mount → nothing visible. Generic `terminal:mount` failure = permanently blank pane (worktree errors DO get an error card — reuse that pattern). | `Dashboard.tsx:1223,1113-1115,1059-1062,932-933`; `WorkspaceView.tsx:440-442` (vs card `:203-271`) | open |
| ERR-5 | H | Archive flow destroys state optimistically BEFORE the fallible IPC: `terminal.destroy` (kills live claude) + purge of activity/title/git/PR/footer caches all run before `await workspaces.archive`; catch only console.errors. Failure → workspace still listed, session dead, no error. Main already destroys the surface post-archive (`index.ts:578`) — the renderer pre-destroy is redundant; reorder. | `Dashboard.tsx:1084-1115` | open |
| ERR-6 | M | Corrupt `overrides_json` silently degrades a workspace's launch: parse-catch → treated as empty, **no log**, and `recomputeDirty` compares two snapshots that both lack the overrides so even the "Restart to apply" chip can't fire. Diag-log with project/workspace id. | `claudeProjectSettings.ts:22-28`, `claudeWorkspaceSettings.ts:22-28`; `index.ts:586` | open |
| ERR-7 | M | Session-list DB writes swallow per-row failures inside transactions with no count/log — systematic failure (schema drift; precedent at db.ts:51-55) = silently empty/stale sessions list, indistinguishable from "no sessions". Emit one anomaly event when failures > 0 per batch. | `src/main/sessions.ts:663-687`, `:819-825`, `:900+`, `:642` | open |
| ERR-8 | M | commandServer outer catch swallows handler-plumbing errors with zero log (inner dispatch errors are returned `{ok:false}` — fine). CLI client hangs/drops with no app-side record. | `src/main/commandServer.ts:1208-1212` | open |
| ERR-9 | M | `isWorktreeDirty` treats git timeout/lock-contention as "clean" (any failure → `false`). Actual deletion is still guarded by `git worktree remove` w/o `--force`, but the phase-2 removal loop in projects:remove catches-and-continues (console-only) then cascade-deletes rows → orphaned dirty worktree dirs with their workspace rows gone. Distinguish ENOENT (clean) from timeout/error (dirty); diag-log phase-2 failures. | `src/main/worktrees.ts:227-237`; `index.ts:1195-1220` | open |
| ERR-10 | M | No top-level React error boundary: `main.tsx` renders `<App/>` bare (only the overlay window has one). Render throw → React unmounts the root → permanent white window over a still-alive native terminal. | `src/renderer/src/main.tsx:17-19`; contrast `overlay/OverlayErrorBoundary.tsx` | open |
| ERR-11 | L | `ensureManagedHooks` can clobber a valid-JSON-but-non-object `~/.claude/settings.json` (array/string top-level → treated as `{}` → atomic write replaces file with hooks-only object). Bail like the parse-failure path. | `orpheusNotify.ts:347-362`, write `:415-417` | open |
| ERR-12 | L | brew/git update spawns have no kill timeout (missing-binary and exit-code semantics ARE handled) — a stalled network mount leaves the update check in `checking` forever, no cancel. ~60 s timer. | `src/main/updates.ts:76-100,118,258` | open |

Verified fine: torn-read handling of session files (last-good map, deduped anomaly events) is exemplary; secrets never logged (keys-only mount log, redacting audit log, settingsJson verified secret-free); `execFile` everywhere with timeouts on git/claude probes; SHELL validated before spawn; worktree-create rollback on DB failure; multi-step writes transactional; loading-overlay 10 s fallback with anomaly event.

## 6. Open-source readiness / docs

| ID | Sev | Finding | Evidence | Status |
|---|---|---|---|---|
| OSS-1 | H | LICENSE forbids the product's own distribution model: clause 1 restricts to "personal evaluation of the source code", clause 2 prohibits copying — yet the app ships via a public brew tap for people to *run*. Every brew user technically violates the license. Add an explicit grant to run official binary releases. | `LICENSE:11-13` vs `README.md:26-31` + cask pipeline | open |
| OSS-2 | H | README has no end-user install section: brew is the only channel and the `brew install --cask` command appears nowhere; no screenshots either (single highest-leverage README addition for a GUI app). | `README.md` | open |
| OSS-3 | H | Fresh clones can break through no fault of the user: postinstall `fetch-libghostty.sh` downloads from a third party's rotating "storage" releases that 404 weekly (repo's own CI comment admits it). Mirror the pinned zip to an amitray007-owned release; document the `--ignore-scripts` escape hatch. | `scripts/fetch-libghostty.sh:25-27`; `.github/workflows/ci.yml:18-20,33-34` | open |
| OSS-4 | M | SECURITY.md says distribution is via a "**private** Homebrew tap" — it's public. | `SECURITY.md:49` | open |
| OSS-5 | M | `.codex/agents/audit-claude-env-vars.toml` is tracked and visibly corrupted by a global Claude→Codex find/replace: `https://code.Codex.com/docs/en/env-vars.md`, `.Codex/snapshots/…`, "run `Codex --help`". | `.codex/agents/audit-claude-env-vars.toml` | open |
| OSS-6 | M | GPL-3.0 ghostty shell-integration scripts bundled inside an all-rights-reserved app — TPN discloses it, but the aggregation-not-derivative rationale is unexamined in-repo. Document the position. | `THIRD_PARTY_NOTICES.md:15`; `resources/ghostty/ghostty/shell-integration/` | open |
| OSS-7 | M | No `engines` field, no pinned bun (`packageManager` absent; CI floats `latest`, CI-11); scripts invoke bare `node`, stack assumes Node 22/Electron 39 ABI. | `package.json` | open |
| OSS-8 | L | THIRD_PARTY_NOTICES stale: references `packages/ghostty-native` (actual: `ghostty-surface`); lists `@floating-ui/react` + `@electron-toolkit/preload` which are not dependencies. | `THIRD_PARTY_NOTICES.md:11,17,23` | open |
| OSS-9 | L | Personal-machine path in a tracked comment: `/Users/maverick/.claude/worktrees/xterm-experiment`. | `src/main/claudeProjectDir.ts:6-7` | open |
| OSS-10 | L | Dep placement: `react`/`react-dom` in devDeps while renderer-only `@phosphor-icons/react`, `geist`, `minidenticons`, `@web-kits/audio` sit in `dependencies` (needlessly packed into asar node_modules — electron-vite externalizes `dependencies` for main only). Dead `pnpm.onlyBuiltDependencies` block in a bun repo. | `package.json:50-58,80-81,86-91` | open |
| OSS-11 | L | Issue templates are legacy `.md` (no forms, no `config.yml` → blank issues allowed, no security-contact link); no CODEOWNERS (optional, single maintainer). Intel support claimed in README but never CI-tested (xcframework slice is arm64+x86_64). | `.github/ISSUE_TEMPLATE/`; `README.md:30` | open |
| OSS-12 | L | Stale private-era framing in tracked files: `.impeccable.md` ("personal, single-user tool… deferred until share-ready"); `docs/learnings/overlay-child-window-macos.md` references a gitignored plan doc (dead link publicly). | `.impeccable.md`; `docs/learnings/overlay-child-window-macos.md:3-5` | open |

Verified fine: LICENSE/SECURITY/CONTRIBUTING/CoC/TPN/templates/FUNDING/dependabot all exist and are substantive; no secrets in tracked files (grep sweep for token/key patterns clean); ARCHITECTURE.md is an accurate human-facing companion to CLAUDE.md; deps are current-gen with no unused entries; naming ("Orpheus" prose / `orpheus` identifiers) consistent.

---

## Suggested order of attack

1. **Quick wins (≤1 day, each immediately felt):** eslint-ignore `docs/` (ACT-1) · dedup version warning (ACT-2) · async `listCommits` (ACT-5) · `staging` in ci.yml push triggers (CI-1) · `.catch`+dialog on whenReady (ERR-1) · SECURITY.md/TPN/codex-toml/username fixes (OSS-4/5/8/9) · clear `pendingHide` in overlay `show()` (RACE-4) · reconcile max-wait (RACE-5) · fix the three lying comments (CONV-1/2/3) · theme DEFAULT `'dashboard'`→`'sessions'` (MDB-13).
2. **Correctness (wave-2 highs, before they bite):** re-validate workspace across `terminal:mount` awaits + orphaned-snapshot cleanup (RACE-1) · generate the heal-rebuild table from `WORKSPACES_SCHEMA_SQL` (MDB-1) · `useUiState` store fixing the stale-mirror live bug (RQ-1/2) · mount-epoch visibility guard (RACE-2/3) · workspace-scoped resource registry + `overlayFallbackTimers` hole (MDB-5) · migration version stamping (MDB-2) · CLI send mutex (RACE-10).
3. **CI hardening:** eslint in lint-staged + commit-msg hook (CI-10) · native path filter (CI-3) · migration smoke in CI (CI-5) · `noImplicitAny: true` (CI-2) · pin bun + action SHAs (CI-9/11) · first `bun test` over `composeClaudeLaunch` (CI-4) · type-aware ESLint for src/main — would have caught RACE-12 and friends (CI-16).
4. **Structural debt (dedicated efforts):** typed IPC ChannelMap incl. push channels + one error-shape convention (DUP-3/6, MDB-9/10) · merge twins (DUP-1/2/4, MDB-13 footer tables) · `useGlobalSettings`/`useUiState` hooks (DUP-5, RQ-10) · `createPerKeyStore` factory (RQ-3) · split index.ts / Dashboard.tsx / startCommandServer (STR-1/2, MDB-6/16) · trim hook stack (ACT-3) · delete dead exports + `#if 0` block (DEAD-2, MDB-14) · renderer error surface + boundary (ERR-4/10) · archive-flow reorder (ERR-5) · `claudePaths.ts` + `UI_STATE_DEFAULTS` (MDB-7/8) · hook extractions + contexts (RQ-8/9) · rename archive→delete or annotate vestiges (CONV-5, MDB-4).
5. **Polish:** RuleListEditor keys/focus (RQ-11) · dialog a11y (RQ-12) · `Input` primitive (RQ-13) · status-enum docs in types.ts (CONV-7) · error-message standardization (CONV-8) · dead resources/config cleanup (CONV-14/15) · LIKE escape (MDB-11) · session-file field validation (MDB-15).
6. **Before going public:** LICENSE grant (OSS-1) · README install + screenshots (OSS-2) · mirror libghostty artifact (OSS-3) · GPL rationale (OSS-6).

---

## Wave 2 — code-quality deep dive

_Dimensions: React & renderer patterns (§7), conventions/comments/naming (§8),
async/race correctness (§9), main-process/DB craftsmanship (§10). All complete._

### 7. React & renderer patterns (wave 2)

| ID | Sev | Finding | Evidence | Status |
|---|---|---|---|---|
| RQ-1 | H | **Live bug — stale `uiState` mirror.** Dashboard fetches `window.api.uiState.get()` exactly once (`[]` deps) and never calls `uiState.onChanged` (its only subscription is `workspaces.onChanged`). Consequences: (a) the interaction-sounds toggle writes the DB + the settings section's local copy, but `setSoundEnabled`'s only caller is Dashboard's frozen snapshot → **toggle does nothing until relaunch**; (b) `fetchGithubAvatars` passed to Sidebar/MainContent from the same stale snapshot → restart-required; (c) `soundPack` only works live because the settings section hand-calls `setSoundPack(v)` inline — an ad-hoc bridge papering over the missing subscription. Seven independent `uiState.get()` sites use **three different consistency policies** (Sidebar + WorkspaceFooter subscribe correctly; ProjectHeader, OrpheusFooterSection, OrpheusUpdatesSection fetch one-shot). Fix: one `useUiState()` hook / per-key store (get + onChanged once), delete the seven ad-hoc fetches. | `Dashboard.tsx:123-131,149-154,1441,1496`; `OrpheusAppearanceSection.tsx:268-269,282`; `Sidebar.tsx:1065-1067`; `footer/WorkspaceFooter.tsx:62-66` | open |
| RQ-2 | H | Theme has two competing DOM writers: Dashboard writes `documentElement.dataset.theme/accent/fontScale` from its (stale) uiState; OrpheusAppearanceSection writes the same keys imperatively inside `patch()` with a comment asserting "both writers stay in sync" — they don't (RQ-1). Fixing RQ-1 collapses to one writer. | `Dashboard.tsx:136-147`; `OrpheusAppearanceSection.tsx:78-88` | open |
| RQ-3 | H | Six copy-pasted per-key external stores (`gitStore` 106, `prStore` 111, `titleStore` 91, `activityStore` 102, `activityTimeStore` 54, `sleepStore` 71 = 535 lines): identical ~40-line Map+listeners+`useSyncExternalStore` machinery (one comment appears word-for-word in ≥4 files), diverging in easy-to-desync details (shallow-equal guards vs identity-only, monotonic writes, eviction present-or-absent). Fix: `createPerKeyStore<T>({ equals?, monotonic? })` factory (~60 lines); each store becomes 5–10 lines. Highest-leverage refactor in `lib/`. | `src/renderer/src/lib/{git,pr,title,activity,activityTime,sleep}Store.ts` | open |
| RQ-4 | M | Three uncoordinated copies of git status for the same workspaces: the global `gitStore` (pushed via `git.onStatusChanged`); WorkspacesTab's own `gitByWs` useState fetched once per row, never invalidated, not subscribed → its Branch column can disagree with the sidebar hover card at the same moment; ProjectHeader's third local fetch. Fix: all read the existing gitStore. | `Dashboard.tsx:736,783,789`; `WorkspacesTab.tsx:405,553-573,610`; `ProjectHeader.tsx:352,361-373` | open |
| RQ-5 | M | Computed-string dep smuggled into deps: `}, [active.map((w) => w.id).join('|')])` with exhaustive-deps disabled; effect body also reads `gitByWs` (not in deps) so a cancelled-mid-flight row fetch is never retried. Dashboard does the same trick more legibly via a named `workspacesPollKey` useMemo — two idioms for one need. Fixing RQ-4 deletes this effect. | `WorkspacesTab.tsx:443,555`; contrast `Dashboard.tsx:704,826` | open |
| RQ-6 | M | SessionsTab effect chain with a cross-project stale read: `metadataVersion` → `hasAnySessions` refetch → paged fetch; `hasAnySessions` is not reset when `projectId` changes, so on project switch the main fetch fires with the **previous** project's value and can auto-widen the date range from the wrong project's data; each switch costs 3 sequential fetch rounds. Fix: reset on projectId change or fold the has-any probe into the paged IPC response. | `SessionsTab.tsx:109-121,125-139,152-207,181-183` | open |
| RQ-7 | L | Of 20 `exhaustive-deps` suppressions, 17 carry justifying comments (fine); 3 are lint-shaped: Sidebar's hover-close effect calls per-render closures safe only by accident; ActionChip suppresses part of a `useCallback` dep list (future drift invisible). Use React 19 `useEffectEvent` instead. | `Sidebar.tsx:276-285`; `footer/ActionChip.tsx:280` | open |
| RQ-8 | M | Copy-pasted logic that should be hooks: (a) debounced search ×3, character-identical 250 ms setTimeout — with an accidental behavioral divergence (WorkspacesTab lowercases, others don't) → `useDebouncedValue`; (b) hover-card timer machinery ×3 (~60 lines each; the 120 ms/80 ms magic numbers repeated six times) → `useOverlayHoverCard` in overlayClient; (c) inline-rename protocol implemented twice, with `WorkspaceNameCell` receiving two raw setters + commit callback through 7 levels → `useInlineRename`. (Counterexample: `sharedTicker.ts`/`useFooterActions.ts` are well-factored — discipline is inconsistent, not absent.) | `SessionsTab.tsx:100-103`, `WorkspacesTab.tsx:417-421`, `CommitsTab.tsx:150-153`; `Sidebar.tsx:223-310`, `WorkspaceTitleBar.tsx:171-330`, `CollapsedProjectList.tsx:64+`; `WorkspacesTab.tsx:595-602` | open |
| RQ-9 | M | Component API sprawl: Sidebar takes **30 props** (17 `on*` callbacks); MainContent 15 (≥5 pure pass-throughs). Worst chains: `onSelectWorkspace` in 7 files / 30 occurrences (depth 3–4); `fetchGithubAvatars` — one boolean — threaded through 6 files / 20 sites. Exactly one context exists in the whole renderer (`SidebarBoundsContext`), proving the fix pattern is known. A `UiPrefsContext` (or RQ-1's store) deletes the avatar chain; a `WorkspaceActionsContext` collapses ~12 of Sidebar's 17 callbacks. Also: `allWorkspaces` drilled Dashboard→WorkspaceView→WorkspaceTitleBar solely to `find()` a fork-parent's name (defeats memoization) → resolve the name in main or a lookup hook. | `Sidebar.tsx:~40-80`; `WorkspaceTitleBar.tsx:335-340` | open |
| RQ-10 | M | State design: WorkspacesTab holds 11 `useState` forming two coherent machines (table-query state where "filter change ⇒ page reset" lives in scattered `setActivePage(1)` calls; row-edit state) — wants `useReducer`; SessionsTab repeats the shape with 13 `useState` and encodes first-load-vs-refetch in a comment instead of a status union. The `loading`+`setError` boolean/string pair recurs in 10+ settings files — the idle/loading/error union belongs in whatever shared loader replaces the DUP-5 scaffold. | `WorkspacesTab.tsx:399-416`; `SessionsTab.tsx:71-109,80-83` | open |
| RQ-11 | M | `RuleListEditor`: index keys on editable, removable controlled inputs (delete row N → every following input re-keys, focus lands on the wrong row; Escape-in-input deletes, compounding it); same pattern in ClaudeDeveloperSection. Plus `addItem` focuses via a **document-wide** `querySelectorAll('[data-rule-input]')` → with two editors mounted (permissions allow/deny), adding to the first focuses the last input of the *second*. Fix: `crypto.randomUUID()` ids on add; scope the query to a ref. | `settings/primitives.tsx:579-580,546,555-560`; `ClaudeDeveloperSection.tsx:62,70,97` | open |
| RQ-12 | M | Modal a11y/focus: `ui/Overlay.tsx` handles Escape + outside-click but has no `role="dialog"`, no `aria-modal`, no focus trap, no initial focus, no restore — ConfirmModal opens with focus behind the scrim (Tab walks the occluded page; Enter triggers whatever had focus). Overlay-window ConfirmModal likewise never autofocuses the confirm button, so Enter-to-confirm fails. `lib/useFocusOnMount.ts` already exists — wire it + a ~20-line trap + dialog roles. | `ui/Overlay.tsx:41-56`; `overlay/kinds/ConfirmModal.tsx`; `overlay/OverlayRoot.tsx:144-152` | open |
| RQ-13 | L | Tailwind: the text-input recipe is duplicated ~12× in four drifting variants (`px-3 py-1.5 rounded-md text-xs bg-surface-raised border …` ± `w-64/w-full/font-mono/transition-colors`) — there is a `Button` primitive but **no `Input`**; accent-button (4×) and accent-chip (4×) strings bypass `Button`. Fix: add `Input` primitive + a chip variant; no CVA dependency needed. | `settings/*` (dedup scan: 4+3+3+2 occurrences) | open |

Verified fine (don't re-audit): drag-reorder lists correctly keyed by id; callback naming consistent (`onX` props / `handleX` locals); dark mode fully token-based via `[data-theme]` (zero `dark:` divergence); inline styles disciplined (38, almost all forced); no virtualization needed (everything paginates at ≤20 rows); clickable-div a11y clean (106 aria-labels; TopBar exemplary).

### 8. Conventions, naming, comments, in-code docs (wave 2)

Marker inventory (real counts, first-party code): TODO 4 · FIXME 0 · HACK 0 · XXX 0 ·
"for now" 5 · "legacy" ~13 relevant · "no longer" 11. Dominant failure mode is *stale*
comments, not noise comments.

| ID | Sev | Finding | Evidence | Status |
|---|---|---|---|---|
| CONV-1 | M | Stale "until it lands" comment guarding a dead fallback: Dashboard keeps a legacy per-event activity channel with `// used until main-layer lands onActivityBatch` — but `onActivityBatch` HAS landed (implemented in preload, typed in index.d.ts, emitted by activitySink) and Dashboard even duck-type-probes it through a local structural cast instead of trusting the typed contract. Delete the fallback + probe; then remove activitySink's "legacy per-event" fan-out (DEAD-2). | `Dashboard.tsx:172-178,189,205`; `preload/index.ts:282`; `activitySink.ts:8,43,56` | open |
| CONV-2 | M | Module header contradicts itself and the architecture: `sessionState.ts` opens with "Shadow-mode session state service (Phase 1)" then says "This module is the SOLE authority for workspace state" three lines later. Phase 2 cutover already happened — the first line a contributor reads about the status authority says it's an experiment. | `src/main/sessionState.ts:2` | open |
| CONV-3 | M | User-facing settings-search copy lies: searchIndex says "Dark is the only available theme for now. Light and System auto-switch are planned" — but three themes ship (`midnight\|daylight\|eclipse`, in the picker and DB CHECK). Concrete instance of the DUP-7 drift risk, shown to users in search results. | `searchIndex.ts:1272` vs `types.ts:172`, `OrpheusAppearanceSection.tsx:13-15`, `db.ts:1531` | open |
| CONV-4 | M | Singular/plural IPC namespace split in one domain: `workspaces:create/open/setPinned/archive/rename/convertToLocal` vs `workspace:close/reopen/isDirty` — the only such collision among 34 channel namespaces; grepping either prefix misses half the surface. Consolidate on `workspaces:`. | `index.ts:1367,1376,1396` | open |
| CONV-5 | M | archive/remove/delete/close all mean "delete" somewhere: "Archive" hard-deletes since v34 (`archiveWorkspace` → `{ archived }`), projects use `projects:remove` → `deleteProject()` → `{ deleted }`, and `workspace:close` is the *non*-destructive one. Residue: `WorkspaceStatus` still contains vestigial `'archived'`, `archived_at` still synced with an unreachable status, `listWorkspacesForProject` still takes an `'archived'` scope. Rename to `workspaces:delete`/`deleteWorkspace` in one pass or annotate the vestiges. | `workspaces.ts:314-337,450-478,264`; `types.ts:679` | open |
| CONV-6 | M | Duplicate functions in one file: `getProject` and `getProjectById` in projects.ts — identical signature and SQL, differing only `row ?` vs `row != null ?`; both have live callers (index.ts ×5 vs commandServer.ts). Implies a semantic difference that doesn't exist. Delete one. | `src/main/projects.ts:140,165`; `commandServer.ts:499` | open |
| CONV-7 | M | The two status enums driving the whole UI are the only undocumented ones in types.ts: `WorkspaceStatus`/`WorkspaceActivityDetail` carry zero comments on their mapping (`in_progress→working`, `awaiting_input→ready`…), that `awaiting_input` renders as "Ready", or that `'archived'` is vestigial; `SessionStatus`'s default `'in_review'` likewise unexplained (the mapping lives only in `orpheusNotify.computeDetail` + CLAUDE.md prose). types.ts is otherwise B+ (SessionUsage/TerminalMountResult/WorkspaceRecord exemplary). Six lines of doc comments fix it. | `src/shared/types.ts:679-681,699`; `db.ts:34` | open |
| CONV-8 | M | "Not found" errors untraceable + inconsistent: `workspace not found: ${id}` thrown 9× (lowercase), `Project not found:` 6× (capital), two one-off prefixed variants, and `'args.id is required'` thrown from 6 different action handlers with no hint which action. A diag line can't be grepped to a throw site. Standardize `<channelOrFn>: <noun> not found: ${id}`. | `workspaces.ts`, `actions/*`, `projects.ts:177` | open |
| CONV-9 | L | Locally-true, globally-misleading comment: claudeSettings' "Custom env vars — merged last; user's keys win" is true within `composeClaudeLaunch`, but `buildMountEnv` merges `authEnv` after `launch.env`, so a user-defined `ANTHROPIC_API_KEY` silently loses. Append the exception to the comment. | `claudeSettings.ts:1023`; `orpheusSurfaceAdapter.ts:95-123` | open |
| CONV-10 | L | settings-vs-config vocabulary split: channel `ghosttySettings:*` served by module `ghosttyConfig.ts` returning `GhosttyUserConfig`; lone orphan channel `config:openFolder` beside `orpheusConfig:*`/`claudeSettings:*`/`uiState:*`. Pick one word per domain; re-home `config:openFolder`. | `index.ts:1104,1464`; `preload/index.ts:369-372` | open |
| CONV-11 | L | Workspace-creation functions live in the sessions module: `sessions.ts` exports `createWorkspaceResumingSession`/`createWorktreeResumingSession` while `workspaces.ts` exists; bare `sessionId` (76 uses) sometimes means registry-file id, sometimes DB row id. Move the two functions or add a placement note. | `src/main/sessions.ts` | open |
| CONV-12 | L | Three file-naming conventions in `components/`: kebab-case registry-vendored files (`dotm-square-12.tsx`) beside PascalCase `Overlay.tsx` in the same `ui/` dir; dot-suffixed helpers (`dashboard.helpers.ts`) beside camelCase `resolveWorkspaceName.ts`. The kebab=shadcn-registry rule is real but documented nowhere. One-line convention note in CONTRIBUTING. | `components/ui/`, `components/dashboard/` | open |
| CONV-13 | L | Timestamp units documented inconsistently in types.ts: `dateFrom` says "epoch ms" explicitly but `addedAt`/`lastOpenedAt`/`createdAt`/`pinnedAt`/`statusUpdatedAt` are bare `number` (all verified Date.now() ms). One section-level doc note. | `src/shared/types.ts` | open |
| CONV-14 | M | 5 unreferenced files ship in `resources/` (menu-bar icons ×4 — leftovers of a removed tray feature — plus `icon-source.png`): zero references in src/packages/scripts/builder configs, and `resources/` ships in every dmg via the `files` globs. Confirm no dynamic path, delete (move `icon-source.png` out of resources if it's the design master). | `resources/menu-bar-icon*.png`, `resources/icon-source.png` | open |
| CONV-15 | L | Config debris: `components.json` aliases point at nonexistent `@/lib/utils` + `@/hooks` (breaks the day someone runs `shadcn add`); `build:win`/`build:linux` scripts are template leftovers that cannot work (mac-only NAPI addon); `doctor.config.ts` header claims react-doctor runs "via npx" but it's a devDep run directly. | `components.json`; `package.json`; `doctor.config.ts` | open |
| CONV-16 | M | Grab-bag modules (worst 3): `index.ts` (131 handle registrations, 34 local fns, 81 section dividers, 0 exports — the dividers prove the domains are known, they never got files; = STR-1); `orpheusNotify.ts` (16 exports across 4 unrelated concerns — status dispatch, watchdogs, hook-shim mgmt, socket server; post-Phase-2 the name no longer describes its main job); `sessions.ts` (DB queries + jsonl import + workspace creation + context-budget math). Cleanest for contrast: `claudeProjectDir.ts`, `activitySink.ts`, `db.ts` (2,497 lines but 3 exports, coherent). | `src/main/` | open |
| CONV-17 | L | CLAUDE.md migration convention followed only half the time: "add column to CREATE TABLE for fresh installs, then defensive ALTER" — `workspaces` CREATE includes later columns but `projects` CREATE lacks `expanded_in_sidebar`/`sort_order`/`github_*` (ALTER-only). Runtime-safe (fresh installs replay all migrations), but the documented pattern and the code disagree — update tables or soften the doc. | `db.ts:19-27` vs `:522,963,1556`, `:70` | open |

Verified fine: tricky invariants (env-layer order, project-delete teardown ordering, migration heal-before-fast-path, hide≠destroy overlay lifetime, context-token semantics) are all documented at point of use — unusually well; get/fetch/load/read/list prefix discipline consistent across 54 src/main exports; shell scripts shellcheck-clean (and `orpheus-claude.sh`'s zsh choices are deliberate + commented); `[module]` log-prefix convention consistent; `.claude/` agents/snapshots all referenced; the 4 first-party TODOs are accurate, incl. an exemplary `TODO(security)` in orpheusNotify.

### 9. Async correctness & races (wave 2)

| ID | Sev | Finding | Evidence | Status |
|---|---|---|---|---|
| RACE-1 | H | **Archive during in-flight `terminal:mount` resurrects the worktree and spawns a zombie claude.** `terminal:mount` captures `getWorkspace(id)` up front, then awaits `reconcileWorktree` (seconds, behind `withRepoLock`) / `getUserShellPath()` and never re-validates. Interleaving: mount awaits reconcile → user archives (same repo lock, wins first): worktree removed, row DELETEd, surface torn down → mount's reconcile then runs: worktree missing → **state 5 recreates it on disk** → mount continues: `setWorkspaceCwd` writes against a deleted row, `addon.mount` spawns a fresh claude with no UI entry (leaks until quit), `launchSnapshots.set` re-seeds state teardown just evicted (`recomputeDirty` only `continue`s on missing workspaces → permanent stale entry). Fix: re-check the workspace after every await in `terminal:mount` (bail before `addon.mount`), or a per-workspace generation counter bumped by destroy/archive; make `recomputeDirty` delete orphaned snapshots. | `index.ts:2074-2294,555-584,592-593`; `workspaces.ts:337-371`; `worktrees.ts:532-650` | open |
| RACE-2 | M | Stale mount completing after navigation steals visibility and leaves the destination blank: `mount(A)` in flight → user switches to B (hide(A) no-ops, B mounts, visible) → A's awaits resolve → `addon.mount(A)` + `setVisibleWorkspace(A)` hides B (+ loading overlay flashes over B) → renderer guard hides A, but native `Hide` only clears `g_visibleWorkspaceId` and **never re-promotes B** → blank terminal until the freeze watchdog recovers. Fix: main-side mount-epoch/currently-viewed check before `addon.mount`, or renderer re-mounts the active workspace on the `!activeRef` path. | `index.ts:2074-2294`; `WorkspaceView.tsx:416-423,716-723`; `addon.mm:3288-3313` | open |
| RACE-3 | M | `backgroundMountWorkspace` (CLI `--background`) momentarily steals visibility with no restore: `mount(B)` promotes B (hiding the viewed workspace X), then `hide(B)` clears the visible id — X stays hidden; only the liveness watchdog recovers. Same native asymmetry as RACE-2. Fix: a `visible:false` mount option in the addon, or re-promote `selectedWorkspaceIdRef.current` after the hide. | `Dashboard.tsx:531-573` | open |
| RACE-4 | M | `loadingOverlay.show()` doesn't cancel a pending min-show hide: navigate away <250 ms after mount arms `pendingHide`; navigate back → `show()` hits the active-entry branch (updates copy only) → `pendingHide` fires ~200 ms later, killing the overlay mid-worktree-reconcile — exactly the blank pane it exists to mask. Fix: clear `pendingHide` in the active-entry branch. | `loadingOverlay.ts:62-97,100-125` | open |
| RACE-5 | M | `scheduleReconcile` debounce has no max-wait: every call (including the 2.5 s interval's!) resets the 75 ms timer, so during sustained fs-event storms (N busy sessions writing status files >1×/75 ms) reconcile is starved indefinitely and the "backstop" interval backstops nothing. Fix: max-wait (~500 ms) or have the interval call `_runReconcile()` directly. | `sessionState.ts:268-281,182-184` | open |
| RACE-6 | M | `forceReconcile()` bypasses the single-flight and can re-enter `reconcile()` from inside itself: reconcile → dispatch → statusObservers fire synchronously → commandServer `ws wait` observer awaits `forceReconcile()` → re-entrant `reconcile()` mutates `liveSessionMap`/`lastRawActed` mid-loop. Quasi-benign today only because `reconcile()` happens to contain zero awaits; the first `await` added inside it turns this into a real interleaved read-modify-write. Fix: route `forceReconcile` through the single-flight; at minimum a re-entrancy guard + "must remain await-free" comment. | `sessionState.ts:222-224`; `commandServer.ts:839,1006-1028`; `orpheusNotify.ts:195-201` | open |
| RACE-7 | L | `startGitWatch` registration races teardown for **closed** workspaces: close during the async `rev-parse` → teardown's `stopGitWatch` no-ops (not registered yet) → rev-parse resolves → guard passes (`getWorkspace` is truthy — close keeps the row) → leaked FSWatcher + pushes for a dead view until a later destroy. Fix: guard on `closedAt` too, or a per-call token invalidated by `stopGitWatch`. | `git.ts:387-464` (guard `:396-398`); `index.ts:537-553` | open |
| RACE-8 | L | `fetchWorkspacesForProject` has no stale-response guard and its catch clobbers good data with `[]`: an in-flight list fetch resolving after a `workspaces:onCreated` prepend replaces the array with the pre-insert list (new workspace vanishes until next fetch). Fix: per-project monotonic request id; keep previous data on error. | `Dashboard.tsx:398-406,288-303` | open |
| RACE-9 | L | Navigation side effects inside a `setWorkspacesByProject` updater: `setSelectedProjectId`/`setView`/`uiState.update` IPC run inside the updater (which returns `prev`) — updaters must be pure; StrictMode double-invocation → double navigation + duplicate IPC. Fix: read from `workspacesByProjectRef`, navigate outside. | `Dashboard.tsx:332-359` | open |
| RACE-10 | M | No per-workspace injection mutex in `openAndSeed`/`sendToWorkspace`: two concurrent CLI sends both pass the injectable poll then interleave `sendInput`→`delay(150)`→`submit` — A.submit submits the concatenation of A+B; B.submit fires into an empty input. Fix: per-workspace promise-chain mutex (the `withRepoLock` pattern). | `index.ts:2854-2953,2955+` | open |
| RACE-11 | L | Sequential awaits that should be parallel: `projects:remove` phase-1 runs `isWorktreeDirty` serially per worktree behind a modal; `refreshGitForDir` awaits per client in a loop (also PERF-5). Reverse case: Dashboard fires git+`gh` lookups for **every** unfetched workspace at once with no concurrency cap. Fix: `Promise.all` for the first two; a 4–6-wide limiter for the third. | `index.ts:1195-1204`; `git.ts:300-335`; `Dashboard.tsx:~775-800` | open |
| RACE-12 | L | Floating `actions:subscribe` invoke in preload: neither awaited nor caught (unhandled rejection if main throws), and `dispose()` can send `actions:unsubscribe` before the subscribe resolves → main-side subscription leak. Fix: retain + `.catch` the promise; chain dispose on it. | `preload/index.ts:594-622` | open |
| RACE-13 | L | Git watcher dedup-key comment lies: claims "resolved absolute git dir … handles worktrees" but the key is `resolve(cwd)`, so worktrees sharing a `.git` never share an entry (safe, but the claim is wrong). Fix comment or key. | `git.ts:403-405` | open |

Verified fine: `withRepoLock` is a correct async mutex (chain survives rejections); activitySink batching loses no trailing updates; `_runReconcile` single-flight + trailing-dirty is correct for the watcher path; preload `on*` helpers remove exactly the wrapped listener (all spot-checked renderer subscriptions unsubscribe); WorkspaceView's renderer-side mount/hide sequencing is solid (StrictMode guards, rAF/timer cleanup, `activeRef` re-checked after every await) — the residual gap is main-side (RACE-2/3); no compose-vs-settings-update missed-dirty window; `stopGitWatch` refcount and timer hygiene correct; auth env deliberately outside dirty-tracking; double-archive benign.

### 10. Main-process & database craftsmanship (wave 2)

| ID | Sev | Finding | Evidence | Status |
|---|---|---|---|---|
| MDB-1 | H | **`healWorkspacesCheck` rebuild table is a drifted twin of the real schema — silently drops the four v64 columns.** The boot-time heal path (runs every boot, before the version fast-path) rebuilds `workspaces` via an inline `CREATE TABLE workspaces_new` that ends at `last_title`, omitting `closed_at`, `parent_workspace_id`, `worktree_parent_cwd`, `worktree_branch`; the copy-INSERT doesn't select them either. If the CHECK is ever judged stale on a v64+ DB (e.g. a future status-enum edit forgetting the heal's strings), the rebuild destroys closed/worktree/lineage data. Fix: generate `workspaces_new` from `WORKSPACES_SCHEMA_SQL` + `pragma_table_info`, or a loud cross-reference comment. | `db.ts:2446-2484` vs `:56-78`, heal call `:435` | open |
| MDB-2 | M | Fresh installs stamp `CURRENT_VERSION` **before** migrations run: `if (!row) INSERT schema_version VALUES (CURRENT_VERSION)` right after base CREATEs, then all 65 historical blocks still execute with per-step bumps suppressed. If any block throws mid-way on a fresh DB, the DB already claims fully-migrated and the fast path skips repair forever. Also ~60 repetitions of the `if (row) UPDATE schema_version` boilerplate → `runStep(n, fn)` helper; stamp after the loop. | `db.ts:454-456,439` | open |
| MDB-3 | M | `stale_after_minutes` exists only as an ALTER, not in the fresh-install CREATE — proof the CLAUDE.md migration convention has gone load-bearing-optional (works only via MDB-2's replay behavior). Add to `UI_STATE_SCHEMA_SQL`; audit for other ALTER-only columns (see also CONV-17 for the projects table). | `db.ts:2175` vs `:333-380` | open |
| MDB-4 | M | Archive state encoded twice in the schema: `status='archived'` in the CHECK **and** `archived_at`/`closed_at` columns, hand-synced in `setWorkspaceStatus` ("Sync archived_at with status") — same duplication in `sessions`. This exact drift class already caused the frozen-status bug memorialized at db.ts:51-55. Related: CONV-5. | `workspaces.ts:469-490`; `sessions.ts:1085-1096` | open |
| MDB-5 | M | Per-workspace keyed state sprawl: **19 module-level Maps across 10 files**, torn down by two diverging hand-maintained paths (`teardownWorkspaceResources` covers 10; sessionState self-prunes its 4; the `terminal:destroy` handler re-implements a subset **plus** `overlayFallbackTimers`). **Concrete hole:** `overlayFallbackTimers` is cleared only in the `terminal:destroy` IPC handler — `performArchive`/`performClose` call `addon.destroy()` directly + `teardownWorkspaceResources`, which doesn't touch it → archive during the mount-overlay window leaks a timer that later fires `hideLoadingOverlay` for a dead workspace. This bug class recurs every time a new map is added. Fix: a `workspaceScopedResources.register(...)` registry iterated by one teardown. | inventory: `index.ts:276,277,281,345`; `orpheusNotify.ts:47,48,82,83`; `sessionState.ts:63-69`; `git.ts:287`; `actions/session.ts:50,169`; `actions/subscriptions.ts:31`; +4 more · hole: `index.ts:523-535,537-584,2389-2410` | open |
| MDB-6 | M | `startCommandServer` is a 730-line function containing an inline HTTP server with **duplicated security plumbing**: token auth (`timingSafeEqual`) implemented twice (`/subscribe` and `/cmd`) and the `BODY_SIZE_LIMIT` chunked-read guard twice — a fix to one copy must be remembered in the other. Extract `authenticate()`, `readBody()`, per-endpoint handlers. (Token handling itself is sound: per-boot random, 0o600, length-guarded.) | `commandServer.ts:530-1259`; auth `:573-588` vs `:1098-1110`; body `:604-617` vs `:1119-1132` | open |
| MDB-7 | M | `~/.claude/*` path composition duplicated 25× across 11 files: `settings.json` built independently 6×; `projects/<encoded>` 5×; `.claude/worktrees/<slug>` built by **two different slug implementations** (worktrees.ts and sessions.ts recompute `slugFromPath` independently — real drift risk). `claudeProjectDir.ts` owns only the encoding. Fix: grow it into `claudePaths.ts`. | `worktrees.ts:201,365`; `claudeHooks.ts:92,139`; `orpheusNotify.ts:343,425,464`; `sessions.ts:703,756,1219`; `claudeAgents.ts` ×8; + | open |
| MDB-8 | M | UI-state defaults hardcoded in 4–5 places each: `staleAfterMinutes` 60 in db DEFAULT + uiState + sessionState + orpheusNotify; `statusPollIntervalSec` 1800 in 6 places incl. the valid-interval array duplicated verbatim in main and renderer; `sidebarWidth` 256 ×5; `archivedWorkspaceLimit` 20 ×4. Fix: `UI_STATE_DEFAULTS` exported from `src/shared/`. | `db.ts:353,359,361,1392,1664,2175`; `uiState.ts:120,131,151,187`; `sessionState.ts:482`; `orpheusNotify.ts:123`; `claudeStatus.ts:25-26`; renderer `dashboard.helpers.ts:33,37,60` | open |
| MDB-9 | M | IPC surface shape inconsistency: parameter keys drift (`{id}` vs `{workspaceId}` vs bare positional scalar — `projects:refreshGithub` takes a bare string while every sibling takes an object); **four coexisting error styles**: throw (12 sites), `null`-on-missing (5), `{ok:false,error}` (13), and one `{ok:false,reason}` with a different field name. Every renderer call site needs bespoke handling; new handlers copy whichever neighbor they were pasted from. Fix: document one convention (throw = programmer error, `{ok,error}` = expected outcome); rename `reason`→`error`. Related: CONV-4 (singular/plural), CONV-8. | `index.ts:1108,1127,1168,1277,1370,1671,1748,1959,2045,2088,2927,2953,3029,3077,3150` | open |
| MDB-10 | M | Push-channel literals restated per process: `'workspace:titleChanged'` 5×, `'workspace:activityBatch'` 5×, `overlay:*` family 16× across 4 files — main's `send` literal and preload's `on` literal can typo-drift with zero compile error (distinct from DUP-3's invoke channels). Fix: `src/shared/ipcChannels.ts` const object (fold into the DUP-3 ChannelMap work). | `index.ts`, `preload/index.ts`, `Dashboard.tsx` | open |
| MDB-11 | L | `LIKE` search lacks `ESCAPE`: user-typed `%`/`_` act as wildcards; the leading `%` also means the `idx_sessions_project_title_lower` expression index can never serve this predicate (and no other query uses it). Escape the term; reconsider the index. | `sessions.ts:1049`; `db.ts:1641` | open |
| MDB-12 | L | Statement caching is ad-hoc: 215 `db.prepare` sites, exactly one cached (diagnostics). `getAppUiState()` (`SELECT *`, 24 call sites) re-prepares inside the reconcile transition path and watchdog arming; `reconcile()` re-prepares its workspace query per 75 ms debounce tick. Fix: `memoPrepare` helper or cache the 3–4 provably hot statements. | `uiState.ts:268`; `sessionState.ts:391-397,482`; `orpheusNotify.ts:123`; contrast `diagnostics.ts:41` | open |
| MDB-13 | L | Schema hygiene: boolean typing inconsistent (~60 flags use `INTEGER CHECK(x IN (0,1))` but two are `BOOLEAN` and six INTEGER without CHECK); fresh installs still DEFAULT `last_view_kind='dashboard'` — a value the app immediately coerces away, for a page deliberately removed; `parent_workspace_id` has no REFERENCES/index unlike `project_id`; `footer_actions_{global,project,workspace}` is a three-way structural twin with triplicated CRUD (`remove()` blind-DELETEs from all three) — a single table with `scope`+`scope_id` halves the module. | `db.ts:74,269-314,337-338,366-377,2234`; `footerActions.ts:387-393` | open |
| MDB-14 | M | Native addon top issues: 261-line dead `#if 0` block (the WebContents drag-swizzle machinery) that a *live* comment still points at as "would fix" a real gap — delete, cite the commit hash instead; `Mount()` is ~347 lines with strdup'd env arrays needing `free` on two exit paths (wants RAII/scope guard); ~23 file-scope mutable globals incl. 4 parallel maps hand-synced (a missed erase is a use-after-free, not a leak) → one `SurfaceRegistry` struct; TSFN boilerplate ×5 with convention-based manual payload ownership (verified idiom at 4 push sites — one wrong copy-paste = leak/double-free) → templated `TsfnChannel<T>`; Mount type-checks the first two args then blind-casts option fields (missing field = opaque napi fatal). | `addon.mm:143,147,207,1098-1358,1363-1378,1774,1798-1879,2941-3288,2974-2976,3217-3228` | open |
| MDB-15 | L | External session-file fields consumed via unvalidated `JSON.parse` cast: if claude ever ships `statusUpdatedAt` as an ISO string, staleness math silently becomes `NaN >= threshold` → false → every idle workspace reads `awaiting_input`. One `typeof === 'number'` guard fails loud instead. | `sessionState.ts:326,481` | open |
| MDB-16 | L | Boolean positional flags: `performArchive(id, true)`, `requestOpenWorkspace(id, focus)`, `deriveReason(id, fromTransition)`, addon `forceWake` — ~5 offenders; `{force:true}` object args self-document. Deep nesting: the command-server deps inlined in index.ts declare named helpers at ~14-space indentation with returns at 6–7 levels → hoist to `commandServerDeps.ts`. | `index.ts:340,555,2852,~2990-3160`; `commandServer.ts:785` | open |

Verified fine: **no SQL injection paths** (every dynamic fragment whitelist-derived, all values bound — table ternaries, camel→snake SET maps, `?`/named-param IN-lists all checked); **zero circular imports** (madge over 53 files; nothing imports index.ts back — the setter-injection wiring is why index.ts is huge but acyclic); time handling unit-consistent (epoch ms everywhere, incl. the addon and the external session files — verified live); sessionState maps self-prune; `listAllSessions` slim projection is an exemplary counter-pattern; diagnostics is the one well-tuned hot writer; JSON settings blobs parse-cached, never LIKE-queried; `recomputeDirty` already threads `precomputedGlobal` to avoid N reads; no missing index on `claude_session_id` (only a small-table scan uses it).
