node_modules
dist
out
.DS_Store
.eslintcache
*.log*
vendor/
packages/*/build/
# .claude/agents and .claude/commands ARE tracked; only the per-dispatch
# worktree clones (each one contains a .git pointer file that would otherwise
# be added as a submodule) are ignored.
.claude/worktrees/
