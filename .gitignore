# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.Spotlight-V100
.Trashes
.fseventsd

# Swift / Xcode
.build/
.swiftpm/
*.xcodeproj/xcuserdata/
*.xcworkspace/xcuserdata/
DerivedData/
*.xcuserstate
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# SwiftPM
.swiftpm/
# (Note: do NOT ignore `Packages/` — our local Swift Packages live under
# `packages/` and macOS case-insensitive FS would otherwise hide them.
# Also do NOT ignore `Package.resolved` — pinning the GRDB version
# across machines is the whole point of committing it.)

# AppKit / macOS app
*.app/

# Build outputs
build/
out/
dist/

# Editor / tooling
.vscode/
.idea/
*.swp
*~
.cursor/

# Local secrets / state
*.local
.env
.env.*

# Orpheus runtime state (see architecture.md)
~/.orpheus/
.orpheus/
